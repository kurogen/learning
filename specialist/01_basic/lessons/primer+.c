#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() 
{
	float a,b,c;
	b=5;
	c=0;
	if (c!=0)
	{
		a=b/c;	
		printf("result is %f\n",a);
	}
	elseprintf("error\n");
	return 0;
}
