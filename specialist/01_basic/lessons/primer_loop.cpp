#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() 
{
	int i; //i - иттерация
	for ( i=0 ; i<10 ; i++ )
	printf("i = %d\n",i);
	return 0;
}
